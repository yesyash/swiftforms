import LoginButton from "@/components/login-button";

export const metadata = {
	title: "Login",
};

const LoginPage = () => {
	return (
		<>
			<LoginButton />
		</>
	);
};

export default LoginPage;
